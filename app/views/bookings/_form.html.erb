<%= form_for(@booking) do |f| %>
  <% if @booking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@booking.errors.count, "error") %> prohibited this booking from being send:</h2>

      <ul>
      <% @booking.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="margin-top"></div>

  <div class="field">
    <%= f.label 'Navn' %><br />
    <%= f.text_field :name %>
  </div>
    <div class="field">
    <%= f.label 'Kjønn', :class => 'underline'%><br />
	<% ['Man', 'Kvinne'].each do |gender| %>
		<%= f.radio_button :gender, gender %>
		<%= f.label gender%>
		
	<% end %>
  </div>
    <div class="field">

  	   <%= f.label 'Alder'%><br />
  	   	<%= f.number_field :age, :class => 'age'%>
  	  
  	  
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field  :email %>
  </div>
  <div class="field">
    <%= f.label 'Telefon' %><br />
    <%= f.text_field :phone %>
  </div>
   <div class="field">
  	<br/>
    <%= f.label 'Konsultasjon', :class => 'underline' %><br/>
	<% ['Plastisk kirurgi', 'Hudbehandlinger'].each do |consoltion|%>
	<%= f.radio_button :consoltion, consoltion%>
	<%= f.label consoltion %>
	<% end %>
	
  </div>
  <div class="field">
    <%= f.label 'Mer info' %><br />
    <%= f.text_area :message, :cols => 25, :rows => 6 %>
  </div>
  <div class="actions">
    <%= f.submit 'Send' %>
  </div>
<% end %>


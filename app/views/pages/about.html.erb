<div id="leftSide">
	<h1></h1>
		<% if Sider.find(:all).count > 0 %>

	<% if s =  Sider.find_by_title("Klinikken") %>
<img src="<%= s.picture %>">
</div>

<div id="centerSection">

	<h3> <%= s.title %></h3>
	<%= sanitize s.content %>
	<% end %>
	<% else %>
	<p>Ingenting å vise</p>
	<% end %>
</div>


<div id="rightSide">
		<%= link_to "Klinikken", "klinikken", :class => "at"  %>
		<%= link_to "Ansatte", legers_path%>
		<%= link_to "Bilder", "/gal" %>
</div>

